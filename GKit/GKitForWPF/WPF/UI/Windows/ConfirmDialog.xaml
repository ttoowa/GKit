<Window x:Class="GKitForWPF.UI.Windows.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GKitForWPF.UI.Windows"
        mc:Ignorable="d"
        Width="480" MinWidth="360" MaxWidth="720"
        SizeToContent="Height" MaxHeight="700"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        PreviewKeyDown="OnPreviewKeyDown">
    <Window.Resources>
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="84" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="8,4,0,4" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Foreground" Value="#FFE0E0E0" />
            <Setter Property="Background" Value="#FF4F4F4F" />
            <Setter Property="BorderBrush" Value="#FF6B6B6B" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily" Value="/GKitForWPF;component/WPF/Resources/Fonts/#KoPubDotum_Pro Medium" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                CornerRadius="4"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.86" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.72" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#FFE3AA82" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="2" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Appearance}" Value="{x:Static local:ConfirmDialogButtonAppearance.Primary}">
                    <Setter Property="Background" Value="#FFCC8A5E" />
                    <Setter Property="BorderBrush" Value="#FFE0A77F" />
                    <Setter Property="Foreground" Value="#FFFFFFFF" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Appearance}" Value="{x:Static local:ConfirmDialogButtonAppearance.Destructive}">
                    <Setter Property="Background" Value="#FF8E4B4B" />
                    <Setter Property="BorderBrush" Value="#FFB76565" />
                    <Setter Property="Foreground" Value="#FFFFFFFF" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TitleButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="#FFAAAAAA" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="0,6,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF8E4B4B" />
                                <Setter Property="Foreground" Value="#FFFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="14"
            Background="#FF2E2E2E"
            BorderBrush="#FF6B6B6B"
            BorderThickness="1"
            CornerRadius="7">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="4" Opacity="0.45" Color="#FF000000" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="1" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="OnTitleBarMouseLeftButtonDown">
                <TextBlock Margin="18,0,48,0"
                           VerticalAlignment="Center"
                           Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                           TextTrimming="CharacterEllipsis"
                           Foreground="#FFE4E4E4"
                           FontFamily="/GKitForWPF;component/WPF/Resources/Fonts/#KoPubDotum_Pro Bold"
                           FontSize="14" />
                <Button Width="40" Height="40"
                        HorizontalAlignment="Right" VerticalAlignment="Top"
                        Style="{StaticResource TitleButtonStyle}"
                        FontFamily="Segoe MDL2 Assets" FontSize="13"
                        Content="&#xE8BB;" Cursor="Hand"
                        ToolTip="닫기" Click="OnCloseButtonClick" />
            </Grid>

            <Border Grid.Row="1" Background="#26FFFFFF" />

            <ScrollViewer Grid.Row="2" MaxHeight="390"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <TextBlock Margin="24,24,24,20"
                           Text="{Binding Message}"
                           TextWrapping="Wrap"
                           LineHeight="21"
                           Foreground="#FFCECECE"
                           FontFamily="/GKitForWPF;component/WPF/Resources/Fonts/#KoPubDotum_Pro Medium"
                           FontSize="13" />
            </ScrollViewer>

            <Border Grid.Row="3" Padding="16,10,16,14" Background="#FF363636">
                <ItemsControl ItemsSource="{Binding Buttons}" HorizontalAlignment="Stretch">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Right" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:ConfirmDialogButton}">
                            <Button Content="{Binding Text}"
                                    IsDefault="{Binding IsDefault}"
                                    Style="{StaticResource DialogButtonStyle}"
                                    Click="OnChoiceButtonClick" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </Grid>
    </Border>
</Window>
